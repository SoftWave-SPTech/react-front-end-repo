# Variáveis de ambiente para produção
# Substitua <ELASTIC_IP_NGINX> pelo IP Elastic do NGINX em produção
# Este arquivo será usado durante o build: npm run build

# URL base da API principal (via NGINX)
VITE_API_BASE_URL=http://<ELASTIC_IP_NGINX>

# URLs dos microserviços (via NGINX)
VITE_API_GEMINI_URL=http://<ELASTIC_IP_NGINX>/api/gemini
VITE_API_CONSULTAS_URL=http://<ELASTIC_IP_NGINX>/api/consultas
VITE_API_AUTH_URL=http://<ELASTIC_IP_NGINX>/api/auth

# URL base para documentos/arquivos (via NGINX)
VITE_FILE_BASE_URL=http://<ELASTIC_IP_NGINX>
